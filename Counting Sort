#include <iostream>
using namespace std;

int main() {
    int a[] = {4, 2, 2, 8, 3, 3, 1};
    int n = 7, count[9] = {0};

    for(int i=0; i<n; i++)
        count[a[i]]++;

    for(int i=0; i<9; i++)
        while(count[i]--)
            cout << i << " ";

    return 0;
}
